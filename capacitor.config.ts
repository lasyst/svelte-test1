import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lasyst',
  appName: 'lasyst-svelte',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
